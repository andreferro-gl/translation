base_locale: it
fallback_strategy: base_locale

namespaces: true
flutter_integration: true

input_directory: lib/features
input_file_pattern: .i18n.json
string_interpolation: braces

output_directory: lib/i18n
output_file_name: translations.g.dart
